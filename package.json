{
    "name": "@nikkei/http-helper-monorepo",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "clean": "npm run clean --workspaces",
        "prebuild": "npm run clean",
        "build": "tsc --build",
        "typecheck": "tsc --build",
        "test": "npm run test --workspace=packages/unittests",
        "format": "prettier . --write",
        "format:check": "prettier . --check"
    },
    "devDependencies": {
        "prettier": "^2.7.1",
        "typescript": "^4.7.4"
    }
}
